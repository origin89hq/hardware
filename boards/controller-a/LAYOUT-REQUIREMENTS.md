# Origin89 — PCB layout requirements

For an external layout house. This document says what the boards must satisfy and
**why**, because a rule with no reason attached is the first thing a layout tool
optimises away.

The reasoning behind the circuits is in [`docs/DESIGN.md`](../../docs/DESIGN.md).
This file does not repeat it; it extracts only what constrains placement and
routing.

Rules added or changed for revision B name the issue that argued for them, and
the issue holds the evidence. Where a rule changed, what revision A was built
with is stated beside it, so an export can be checked against either.

Requirements are numbered `A-nn` for the controller; the generator board's
`B-nn` live in [GENERATOR-BOARD.md](../generator-b/GENERATOR-BOARD.md) (§4). Reply against
the numbers. **Any requirement that cannot be met must be
raised before routing starts, not reported afterwards.** Several trade against
each other, and we would rather choose than be told.

---

## 1. Two boards, and why the split exists

A controller that starts a generator at an unattended site four hours from a
road. It sits in a 3D-printed plastic enclosure in an electrical room, beside an inverter,
next to a running generator, through a Canadian winter. Nobody is watching it
fail.

| | Board | Carries |
|---|---|---|
| **A** | **Controller** | STM32G0B1, ESP32-C6 module, three RS-485 channels, CAN, two VE.Direct ports, 1-Wire, FRAM, NOR, power |
| **B** | **Generator** | Two series relays, the hardware run-enable watchdog, the field wiring to the genset |

**The split is a safety decision, not a packaging one.** The run-enable watchdog
lives on board B with the relays. The link between the boards carries only
"please run", so every way it can fail (connector backed out, cable pulled,
cold joint at −30 °C) stops the watchdog being fed and **opens the contact**. A
lost link is the safe state by construction. On one board the identical fault is
a cracked joint that is silent.

The second reason is surge. 60 ft of 18/2 runs to a generator in the garage and
is the dirtiest thing touching this design. On its own board that energy never
enters the board carrying the MCU, three RS-485 channels and CAN.

---

## 2. Deliverables, both boards

| | |
|---|---|
| Source document | EasyEDA Pro project, editable |
| Gerber | RS-274X, complete |
| BOM | With LCSC part numbers |
| Pick & place | Board A, top and bottom |
| Mechanical drawing | DXF: outline, mounting holes, connector positions |
| Fabrication drawing | PDF, with stackup and **achieved** impedance |
| 3D model | STEP |
| NC drill | Excellon, plated/non-plated separated |
| Assembly drawing | PDF, top and bottom, with designators |
| Netlist | IPC-D-356A, for bare-board electrical test |
| Revision block | Document revision and date on the fabrication drawing, matching the schematic and BOM revision delivered |

We want **all** of these, not the two ticked by default on the quote form. The
DXF and STEP are what let an enclosure be chosen without re-measuring, and the
fabrication PDF is the only place achieved impedance gets written down.

---

## 3. Board A — controller

### Mechanical and stackup

| ID | Requirement | Why |
|---|---|---|
| **A-01** | Outline **100 × 125 mm**, 1.6 mm FR-4 | **The board is the master dimension.** The enclosure is 3D printed around the finished board, so the board is sized for what the layout needs and the box follows. It was 100 × 100 (the cheapest fab tier) until the field band ran out of height: connectors at the edge, bus lamps pinned above them and the MCU keep-out above that left **~15 mm of usable band**, while each bus needs a TVS, a transceiver, a jumper and two bias resistors, about **13.4 mm of bodies before clearances**. Past that point every constraint added there only moved the violation somewhere else; one placement bought unambiguous termination jumpers by pushing the RS485-3 surge clamp **17.7 mm** from its own connector, which is a worse board. The extra 25 mm is all field band. One price tier, not a redesign |
| **A-02** | Four **M3** holes, 3.2 mm finished drill, **3.2 mm keep-out radius** measured from the hole centre and applying to **all copper layers and both silkscreens**, centres **4 mm** in from each board edge (**±46, ±58.5 mm** on the 100 × 125 outline) | Nylon standoffs, not brass. Brass under a board with 24 AWG field wiring nearby is a short waiting to happen |
| **A-03** | Mounting holes on **no net**: no plane, no stitching | The enclosure is **plastic**, so there is no chassis to bond to and nothing a standoff could usefully connect. Site earth exists (off-grid systems bond DC-negative to a rod) but that bond is made **once, elsewhere in the system**. A second bond here would create a ground loop through 60 ft of field wiring, which is an antenna for exactly the surge this board is protected against |
| **A-04** | **4 layer.** L1 signal, **L2 solid ground**, L3 power, L4 signal | L2 must be an uninterrupted reference under every differential pair |
| **A-05** | **Route every controlled-impedance pair on L1 only**, and allow **no split, slot or void in the Layer 2 ground plane beneath any differential pair or either crystal**. A pair routed on L4 references the *split power* plane L3 and is not controlled impedance | A return path that detours around a split radiates, and turns a working bench board into an intermittent field board. If unavoidable, ask. Do not route across it |
| **A-06** | All **field** wiring on one edge: 12 V in, the three RS-485 buses (one of them 4-pin, `A-37`), CAN, 1-Wire. One gland plate, one loom, one direction of pull. Rev A also counted the selector here; `A-36` takes it off the board | Measured on rev A: 79.4 mm of connectors against 96 mm of usable edge, so it fits. It only fits because the six signal connectors are **3.5 mm pitch**; at 5.08 mm they need 103.4 mm and the requirement becomes unbuildable at this board size. The 4-pin port adds 3.5 mm: one fits in what `A-06c` measures, three do not (#34) |
| **A-06b** | The short in-enclosure leads (VE.Direct `CN6`/`CN7`, the board-to-board plug `CN9` and the second 1-Wire landing `CN11`) go on the **right** edge, and must not cross the field edge | These are patch leads to gear in the same box, not part of the field loom. Putting them on the field edge would push it past 130 mm and force a larger board for no benefit |
| **A-06c** | The **left edge is the sense and control column**: start-battery sense (`CN12`), SWD (`H1`), tank sender (`CN13`), top to bottom. Rev A had the selector (`CN10`) at the top of that column, withdrawn by `A-36`, and the RTC cell connector (`CN14`) below it; rev B moves `CN14` inboard (`A-25`) and leaves the freed corner to the buck's output copper and ground stitching | **The bottom edge is full.** Six bus connectors use 79.4 mm of the 85.6 mm of clear edge between the corner mounting-hole keep-outs on rev A, 82.9 mm once one RS-485 port is 4-pin; a seventh does not fit. That number is measured. Splitting heavy comms/power one way and thin sense wiring the other is also how a panel gets wired |
| **A-07** | ENIG lead-free, green mask, white silkscreen | Flat pads for the LQFP-64 at 0.5 mm pitch; legible to somebody with a headlamp |

### Impedance

| ID | Net | Target |
|---|---|---|
| **A-08** | `CANH`/`CANL` | **120 Ω** differential ±10 %. ISO 11898 interconnect is a nominally 120 Ω cable terminated 120 Ω at each end, and `R13` on this board is 120 Ω; a 100 Ω trace target would contradict both |
| **A-09** | `RS485_1/2/3_A` / `_B` | **120 Ω** differential ±10 %, or as close as the stackup allows; **report the achieved figure** |
| **A-10** | Both pairs | Coupled the whole run, length-matched within 5 mm, no stubs, and on L1 only per `A-05` |

### Placement

| ID | Requirement | Why |
|---|---|---|
| **A-11** | **TVS at the connector**, before the signal reaches anything else | A TVS downstream of the part it protects protects nothing. The surge has already been through the transceiver |
| **A-11c** | **Verify every 3-pin SOT-23 protection device's land pattern against its datasheet before fabrication.** `D4`/`D5`/`D6` (SM712) and `D7` (PESD1CAN) all have the **common on pin 3**, not the middle pin | Neither library symbol names its pins, so a wrong assumption produces a netlist that passes every connectivity check and protects nothing. Both parts were initially wired with pin 2 to ground. Found by reading the electrical tables, which reference every parameter "pin 1 to 3 and 2 to 3" |
| **A-11b** | **Every field TVS returns to `CN1`'s ground pin by a short, wide, low-inductance path**: multiple stitching vias at each TVS ground pad, and the return must **not** traverse the Layer 2 ground beneath `U7`, either crystal, or any sense divider | "At the connector" alone does not say where the energy *goes*. The enclosure is plastic and there is no chassis, so surge leaves only via the 12 V negative to the bank, which is bonded to earth once elsewhere in the system. A TVS that clamps correctly but dumps its current through the plane under the MCU has moved the problem |
| **A-11d** | Every protection part is one whose surge and ESD ratings **its maker publishes**, and the BOM notes the datasheet revision the rating was read from | Rev A's `D1` (SMBJ18A, OTW) is served only a Taisko datasheet, and `D7` (PESD1CAN, DOWO) has a datasheet that gives two different ESD ratings for the same part. A rating nobody can source is a rating the product cannot claim (#42). Drawn for rev B: `D7` onsemi SZNUP2105L, `D1` Diodes SMBJ33CA; every other new protection part is a Diodes, Nexperia, Littelfuse or Infineon part with its own datasheet |
| **A-12** | Each of the **four** buses (RS-485 ×3 and CAN) is **one tight cluster**: connector + TVS + transceiver + termination resistor + termination jumper + activity LED. **≤ 14 mm pad to pad**, connector A-pin to transceiver A-pin. This one is stated pad-to-pad, not centre-to-centre like the rest of the document, because the number *is* the A/B trace length. **Do not pool parts by type** | A parts-bin layout is tidy and electrically wrong. **Delivered at ≤ 14 mm pad-to-pad on all four buses** (14.2–16.7 mm centre-to-centre, the spread being connector and SOIC body length). 11 mm was attempted to bring the centre-to-centre figure under 15 and the solver could not hold it. Four hard pairs came back at 11.2–13.8 mm, so the limit is set where it actually places. The surge clamp is the part that must be at the terminal: **`D4`–`D7` sit 5.4–8.7 mm from their connectors**. Rev B's 4-pin `CN4` (`A-37`) widens that cluster by 3.5 mm; the pad-to-pad limit is unchanged |
| **A-13** | Each 120 Ω terminator (`R7`, `R9`, `R11`, `R13`) is in series with a **2-pin header and shunt** (`JP1`–`JP4`). Place each header at its own bus cluster, **reachable and readable with the board installed**, silkscreened with the bus name and which state is terminated. Default shipped state: **fitted** | Whether this board terminates depends on where it sits on the bus, and that is decided by whoever pulls the wire at install, not at assembly. A shunt is finger-operable and puts no sliding contact in the bus path, unlike a DIP switch, whose intermittent contact would make termination flicker. **Correction to an earlier draft:** a third 120 Ω terminator takes the parallel load from 60 Ω to 40 Ω. It does not "halve the swing", it loads the driver by half again |
| **A-13b** | Fail-safe bias `R49`/`R50`, `R51`/`R52`, `R53`/`R54` (560 Ω on rev A, raised for rev B by `A-13c`) placed **inside their own bus cluster**, close to the transceiver, not scattered to the power area | **The MAX13487E datasheet makes these mandatory:** *"The pullup and pulldown resistors on the A and B lines are required for proper operation of the device… they function to hold the bus in the high state (A−B > 200 mV) following a low-to-high transition."* The AutoDirection state machine takes the current A−B state as an input, so without bias its direction control runs on an undefined input. The receiver threshold is ±200 mV, so an unbiased idle bus is indeterminate. 560 Ω gives 254 mV idle with both ends terminated (60 Ω), 485 mV with one |
| **A-13c** | Rev B bias sized so the idle bus stays **above the receiver's 200 mV band with three terminators** (40 Ω): about **390 Ω**, which gives 357 mV with two terminators and 244 mV with three at 5 V, 232 mV at `V5`'s 4.75 V minimum. Settle the value on a real cable before ordering, against the bias of the other devices on the site's buses | 560 Ω leaves 254 mV with two terminators and 172 mV with three, inside the MAX13487E's undefined band, and a mid-bus board with its jumper still fitted is the three-terminator case. Two terminators keep only 40–50 mV of margin against noise on a long cable (#26). The silkscreened jumper state of `A-32` is the other half of the defence |
| **A-14** | `LED1`–`LED3` (RS-485) and `LED6` (CAN) visible from the connector edge, each labelled with its bus. Rev B drives every indicator at roughly **0.2–0.5 mA**, the resistor values settled by eye on the rev A bench | They sit on the receiver output, so they show **RX activity**, which is the useful signal: it means something *else* on the bus is talking. Somebody at the panel learns the bus is alive without a laptop. Delivered aligned to within 0.1 mm of their own connector in x. Rev A's parts at about 2 mA are far brighter than an indicator needs in an electrical room, and the transceivers switch the four bus LEDs, so firmware cannot dim them (#35) |
| **A-15** | **`ESP32-C6-WROOM-1-N8`** (Espressif, datasheet v1.x; confirm revision against the shipped part). **No copper on ANY layer** under the module's declared antenna keep-out, and the **antenna at or past the board edge, or the board cut away on both sides of and below it** as Espressif's ESP32-C6 layout guide describes. Espressif additionally require **≥ 15 mm clearance from the antenna to any material inside the finished enclosure**, and end-product range testing; a copper void alone is not sufficient | A module antenna over a ground plane is a module that does not connect, discovered after fabrication. The 15 mm is an enclosure constraint, so it lands on the 3D-printed box as much as on the PCB. Rev A's antenna end sits about 4 mm inside the edge with no cutout; the copper void itself is clean, and the range cost is unmeasured (#16) |
| **A-16** | Keep the antenna end away from every switching regulator, rev B's front-end buck (`A-20e`) included | |
| **A-17** | `X1` within **5 mm** of `U7` pins 10/11 (`OSC_IN`/`OSC_OUT`); `C29`/`C30` within 3 mm either side; **both cap grounds joined to each other first, then one short run to pin 9** (`VSS`, the adjacent pin); no track under `X1` on any layer, unbroken ground on L2 beneath; **≥ 10 mm from the switching inductors `L1`/`L2` and the switch nodes `SW_5V`/`SW_3V3`** | The link runs at 921600 and the clock is what makes that rate honest. **Met in the delivered placement, centre-to-centre: 16.6 mm from inductor `L1`, 49.6 mm from inductor `L2`.** All distances in this document are centre-to-centre unless stated. It took a placement keep-out over the MCU area to get there. Clearance rules alone lost three times to the `V3V3` ratsnest, which drags the 3.3 V buck centre-board because that rail fans out to the MCU, ESP32, FRAM and NOR. **A keep-out anchored at the board centre is not by itself enough, and believing it was cost two more respins of the placement.** The region is anchored at the centre but it does not *hold* the MCU there. The clock block is allowed anywhere inside it, including hard against its own boundary, with a converter legally sitting immediately on the other side. `L1` came to rest **4.49 mm** from `X1` with a 50 mm-wide exclusion nominally protecting it. The rule holds because `U7` is pinned to the board centre. The crystal is tied within 7 mm of `U7`, so it cannot travel more than ~12 mm out, and the 25 mm half-width then guarantees the separation no matter what else moves. **Do not undo it while routing.** Nothing in the netlist or the Gerbers records that this separation is deliberate, so moving `L2` inboard to shorten a trace kills the requirement silently. The victim is a high-impedance node swinging a few hundred mV; the aggressor slams 12 V in nanoseconds at 570 kHz a few cm away. The cost is a pulled clock rather than a dead board. A UART has no clock line, so at 921600 the two ends must agree within a couple of percent across a whole byte. It works on a bench at 20 °C and drops frames in January. If 10 mm from both inductors is ever impossible, prioritise distance from `L2`: it shares a rail with the MCU, so its noise has the shorter path into the ground the oscillator references |
| **A-18** | For **each** TPS54331 (`U1` with `C3`/`L1`/`D3`, `U2` with `C31`/`L2`/`D8`), per the TI datasheet layout section, and for the front-end buck of `A-20e` per its own datasheet: (a) minimise the **input-capacitor → VIN → GND → catch-diode** loop area (the high di/dt loop); (b) place the catch diode and the inductor **immediately at the PH pin**; (c) keep **PH/switch-node copper as small as the current allows** (it is the dv/dt aggressor, not a heatsink); (d) route neither loop's return under a bus pair, either crystal, or any sense divider | "Input cap, inductor, diode in a tight loop" in an earlier draft was electrically imprecise. The inductor is not in the fast loop, the catch diode is |
| **A-19** | `C24`/`C25` within 5 mm of the ESP32 supply pin | It draws 382 mA in bursts |
| **A-26** | **1-Wire protection in order along the path**: the `OW_VCC` limit of `A-24` upstream on `V3V3`; `D9` (ESD clamp) **at `CN8`**, ahead of anything else; `R41` between the field node and `PC4`, **raised on rev B from 100 Ω to a few hundred ohms with the 1-Wire timing still met, and a clamp to `V3V3` and GND at the MCU side whose clamp voltage stays under `PC4`'s VDD + 4 V rating** | Same rule as `A-11` and for the same reason. The clamp behind the series resistor protects the resistor, not the MCU. These probes hang outdoors and in a battery box on cable nobody will inspect again. `D9` is an ESD diode whose clamp reaches 26 V, and 100 Ω in front of a pin rated 7.3 V limits little; `PC4` is FT with no positive injection allowed, so there is no internal clamp to lean on (#31) |
| **A-27** | `R46` (150 Ω, 4-20 mA sense) **at `CN13`**, its return short and not shared with the switching loops | That resistor *is* the measurement. Loop return current wandering through the board becomes a fuel reading that drifts with load |
| **A-28** | `X2` (32.768 kHz) and `C41`/`C42` tight to `U7` pins 4/5, guarded, unbroken ground beneath, **≥ 10 mm from the switching inductors `L1`/`L2` and the switch nodes `SW_5V`/`SW_3V3`** | Same aggressor as `A-17`, and an LSE is *more* fragile than the HSE: lower amplitude, higher impedance, and it has to keep time for years, not just clock a UART |
| **A-29** | `R44`/`R45` with `C39` at `CN12`, and `R42`/`R43` with `C38` beside them off `V_BANK`, ahead of the front-end buck so the house reading is the bank and not the regulated rail (`A-20`), stay **in the left-edge sense column** with `R46`/`C40` (`A-27`); each divider's ground returns to the plane at the divider, and every `AIN_*` trace runs on L1 over unbroken L2 ground, **never through or over either switching loop**. `A-11b` and `A-18`(d) already keep aggressors away from the dividers, and this extends the same protection to the traces, which now cross the board | An earlier draft wanted these dividers at the ADC pins, and the placement that went to routing has them at the sense column instead. Accepted rather than reworked, for two reasons: the left edge is where the sense wiring lands (`A-06c`), and the divided node is not bare; 100 nF across the bottom resistor holds it low-impedance above ~175 Hz (9.1 kΩ Thevenin) while the channels are read at about 1 Hz. What that capacitor cannot do is shunt pickup collected along the trace *after* it, so the trace route is now the requirement: a divider is still a high-impedance node, noise on it is indistinguishable from a real change in battery voltage, and this board decides whether to start a generator on that number |
| **A-30** | The compensation network sits **on the COMP pin**: `R4`/`C9`/`C45` at `U1` pin 6, `R38`/`C37`/`C46` at `U2` pin 6, with `C45`/`C46` the closest of the three to the pin, and their ground returned to the IC's own ground pin rather than into the switching loop | `COMP` is the error amplifier's output and the highest-impedance node on either converter. `C45`/`C46` (100 pF) exist to shunt switching pickup to ground **at the pin**; on the far end of a 20 mm trace they filter nothing and the trace itself becomes the antenna. The values were recomputed against the real filter (see [the compensation section](../../docs/DESIGN.md#the-bucks-compensation-recomputed) of `docs/DESIGN.md`), and a loop with 74° of phase margin on paper is still a ringing rail if this node picks up the switch node |
| **A-32** | Each termination jumper sits **unambiguously above its own connector** (`JP1`↔`CN2`, `JP2`↔`CN3`, `JP3`↔`CN4`, `JP4`↔`CN5`), closer to it than to any other connector by a clear margin, and **each is silkscreened with the bus name, not just its designator** (`TERM RS485-1`, not `JP1`) | The first placement put the whole jumper row about 7 mm left of where it belonged, which left each one roughly equidistant between two buses: `JP2` measured 11.1 mm to `CN2` and 11.8 mm to `CN3`, so three of the four were physically nearest a bus they do not terminate, and `JP1`'s nearest connector was `CN1`, the 12 V inlet. Somebody terminating the EPEver bus reaches for the jumper above `CN2`, fits `JP2`, and terminates the PZEM DC bus instead. **Nothing reports this.** Both buses still talk on a bench; the unterminated one starts throwing CRC errors over 60 ft on a cold night, which is a four-hour drive to diagnose. The LEDs were already immune because they are pinned to their connector's centreline (`A-14`); the jumpers were only softly attracted, and soft lost |
| **A-31** | `C44` (1 µF) within 3 mm of `U7` pin 7 (`VREF+`), **and on rev B a 100 nF at pin 7 and another at pin 8 (`VDD/VDDA`), each as close as the package allows, on the underside beneath the pins if the top is crowded, with the 4.7 µF near pin 8** | `VREF+` is the ADC's reference, and on this package it is a separate pin from `VDD/VDDA` (pin 8) even though both are on `V3V3`. Three of those ADC channels (house bank, start bank, tank level) are inputs to the decision to start a generator. Reference noise does not look like noise in the result; it looks like the battery moved. DS13560 figure 15 asks for 100 nF plus 4.7 µF on VDD/VDDA and 100 nF plus 1 µF on VREF+, placed as close as possible to the pins; on rev A the nearest 100 nF is 6.4 mm away, and how much that contributes to the rail-switch faults of item 8 is not known (#32) |
| **A-33** | **The silkscreen names every connector and its pins in pin order, every jumper with its bus and terminated state, every LED with its function, the debug header's pins, the button, and the board name, revision and date**, with a pin-1 or polarity mark on every connector. The label list lives in `gerber-rules.json` and `tools/validate_silkscreen.py` checks the exported `board.dxf` against it as whole strings, so each label is one text object (`TERM RS485-1` on one line, not two). That check proves presence, not position: that each label sits at its connector with the pins in order is checked by eye on the assembly drawing until the check learns positions from the pick-and-place (#49). The enclosure's port strips follow the board with the rev B export (#20, #34) | Rev A's silkscreen carries 133 designators and nothing else, so on the bare board nothing says what a connector, pin, jumper or LED is for, and a swapped A and B, a reversed start-battery lead or a jumper in the wrong state has nothing to catch it. Board B already writes `CN9`'s pin order on its silkscreen (`B-09`); board A's side does not (#12) |
| **A-34** | **`CN9`'s logic lines are protected at the connector on board A**: a series resistor and a TVS on each of `RUN`, `KICK` and `FEEDBACK` at `CN9`, with a clamp at the MCU side whose clamp voltage stays under the pins' VDD + 4 V rating as in `A-26`, since a surge TVS alone clamps well above it; a pull-down on `RUN` and on `KICK` strong enough to hold board B's `Q2` off against the STM32's 25 kΩ internal pull-up (4.7 kΩ gives about 0.5 V), **placed on the MCU side of the series resistor** so the pin still drives 3.3 V and the cable sees about 3.27 V through 1 kΩ against board B's 100 kΩ; on the cable side the same 4.7 kΩ would leave 2.7 V with 1 kΩ in series and 2.2 V with 2.2 kΩ, at or under the 74HC123's VIH of about 2.3 V at 3.3 V. `FEEDBACK` carries the firmware's internal pull-up (`B-09b`) and gets no pull-down; through 1 kΩ it reads about 0.13 V low with both contacts closed. All three on MCU pins that ST's system bootloader does **not** configure (AN2606 Rev 70 table 111 for the STM32G0B1). `CN9`'s pin order stays `B-09`'s. Design this with board B's `B-03` and its rev B milestone: same cable, same nets | On rev A `PD0`, `PD1` and `PD2` run straight to the cable, and unplugging board B with board A running cost the RTC about 5 s in one step, a connector breaking ground before 12 V being the likely path (#8). Worse, `PD0`/`PD1` are the bootloader's FDCAN1 pins: with an empty flash, which every board has before programming and after an interrupted update, the bootloader pulls `RUN` to 2.1–2.6 V, above board B's gate threshold, and can pulse `KICK`, so the generator's start contact can close with no firmware running. Board B's monostable guards against a stuck controller, not a bootloader that looks like one (#30). The bootloader also configures other rev A nets, `PA2`/`PA3` (USART2, on RS485_1) and `PB6`/`PB7` (I2C1, on the ESP32 link) among the candidates to confirm in table 111, which is why the check is against the table and not the three nets alone |
| **A-35** | **`CN13` is a connector that cannot mate with the 3.5 mm logic sockets** (a different pitch or pin count, or a keyed header), with a TVS at the connector, its loop supply from the rail in `A-20d`, `R46` at the connector as `A-27` and rated for the pin 1 to pin 2 fault, and `R47` raised with a clamp on `AIN_TANK` at the MCU side. Record the minimum sender supply the board supports beside `A-20d`. Drawn for rev B: `CN13` KF2EDGR-3.81-3P, whose 3.81 mm plug does not mate with the 3.5 mm sockets, `D30` SMAJ5.0A on `TANK_SENSE`, `R46` 1206, `R47` 10 kΩ with `D16` BAT54S at `PB2` | Rev A's tank plug fits `CN2`–`CN5`, `CN8`, `CN10` and `CN11`: in `CN10` it puts 12 V on `PB12`, in `CN8` or `CN11` on `OW_VCC` and through `F2` onto the 3.3 V rail. A shorted or miswired sender puts 12 V across `R46`, about 1 W in a ⅛ W part, and `R47` then pulls `PB2` past its 7.3 V limit (#24) |
| **A-36** | **Withdrawn for rev B: board A carries no selector.** `CN10` and its whole network are off the board (`R32`, `R33`, `R59`, `R60`, `D12`, `D13`, `D14`), `PB12` and `PB15` are spare and unconnected, and auto/off/manual is set on `A-42`'s button and kept in FRAM (origin89hq/firmware#60). A board that brings the selector back owes what this rule said: about 1 kΩ in series into the pins, a clamp to `V3V3` and GND below their VDD + 4 V rating, and ESD protection at the connector | Rev A wired the selector straight to the pins behind 10 kΩ pull-ups, which is #31: the pins are FT type, take no positive injection, and that cable is field wiring. Rev B answers it by removing the input rather than guarding it. Auto/off/manual is generator control, the generator is an add-on, and a base controller should not carry a labelled control for equipment that is not fitted. A hardware switch on the generator board, which acts on the relay chain instead of a GPIO, is #55 |
| **A-37** | **One RS-485 port, `CN4`, is 4-pin: `A`, `B`, `GND`, `5V`**, pins 1–3 in the same order as `CN2` and `CN3`, the 5 V pin fed as `A-20c` with an ESD clamp or TVS on it at the connector, and labelled per `A-33`. Check what a 3-pin plug does in the 4-pin socket when pushed in at either end | The PZEM-017's RS-485 side is passive and wants an external 5 V at more than 100 mA over the bus cable; rev A read one on `CN4` at 9600 8N2 as soon as it had 5 V from elsewhere, 119 of 120 polls (#34). Keeping `A`, `B`, `GND` on pins 1–3 keeps one plug order across all three ports. The alternative, all three ports 4-pin at 2.5 mm pitch, also stops those plugs mating with the 3.5 mm sockets and stays open if the part's wire range takes the field cable |
| **A-38** | **VE.Direct ports listen on pin 2**, the producer's TX, with pin 3 left for a controller TX; **a digital isolator per port** whose Victron side is powered from the port's pin 4 within Victron's limit for that pin, 10 mA average and 20 mA for 5 ms bursts, and works at both levels Victron's products drive, 5 V on every MPPT and 3.3 V on the BMV, RS and inverter families; an isolator whose board-side output defaults high while its Victron side is unpowered, so an unplugged port reads idle by the part's own fail-safe and not by a pull-up alone; ESD protection at the connector; pins labelled `RX` and `TX` from the board's own side, as Victron's table reads them, so the port takes Victron's crossed cable. Drawn for rev B: `U17`, `U18` ISO7710 per port, VCC1 from pin 4, output default high while the Victron side is unpowered, pins 2 and 3 clamped by `D31`, `D32` PESD5V0U2BT to the port's own ground, `R14` and `R15` removed | Rev A listens on pin 3, its own TX position, so it works only with a straight cable that joins pin 3 to pin 3, and the silkscreen should not invite that cable back. Victron's MPPTs drive 5 V, which exceeds the FT pins' limit whenever the internal pull-up is on or board A is off, and Victron's FAQ asks for galvanic isolation because the product side has little protection of its own; the same FAQ's port table gives the 10 mA limit and the two logic levels (#27) |
| **A-39** | **ESP32 module lines and straps**: about 1 kΩ in series on every line between the STM32 and the module, in both directions — `ESP_TX`, `ESP_RTS`, `ESP_EN` and `ESP_BOOT` out of the STM32, and `ESP_RX` and `ESP_CTS` out of the module (`R91`, `R92`, added 2026-09-20); pull-ups on IO8 and IO9 to `V3V3_ESP`, not `V3V3` (4.7 kΩ worked on the bench for IO8; confirm against the module datasheet's strapping table), **each at least four times the series resistor in front of the STM32 pin that drives the strap, so the STM32 holds IO9 under the module's VIL of 0.25 × VDD**: 1 kΩ against 4.7 kΩ leaves 0.58 V under the 0.825 V limit, 1 kΩ against 2.2 kΩ leaves 1.03 V and does not; EN held by a voltage supervisor **monitoring `V3V3_ESP`, not `V3V3`**, with an open-drain output, the RC kept, and `PC2` still able to hold EN low, its threshold fixed only after `V3V3_ESP` has been measured under a Wi-Fi burst through the new switch on the rev A rework (item 9 of §5), since Espressif's suggested 3.0 V sits only 0.29–0.34 V under the 3.29–3.34 V the bench saw on `V3V3`; IO12, IO13 and GND on pads as a USB recovery path. The link pins stay as rev A built them: `PB6` to RXD0, TXD0 to `PB7`, `PB3` to IO4, IO5 to `PB4`. Drawn for rev B: `R61` to `R64` 1 kΩ, `R65`, `R66` 4.7 kΩ to `V3V3_ESP`, `H2` pads on IO12, IO13 and GND, `U19` TPS3839 with `RESET#` on the module side of `R61`, the K33 variant at 2.93 V as the placeholder until the burst measurement picks the variant | Rev A's IO8 is unconnected, so the STM32 cannot enter the serial download mode and a blank module needs a wire held on the pad (#6). The STM32's lines go straight to the module, so a pin driven high with the rail off back-powers it through its input protection and the recovery power cycle may not reset it (#17). The two lines the *module* drives were left bare by the same reasoning read only one way: `ESP_RX` and `ESP_CTS` ran straight into `PB7` and `PB4`, so a module whose rail is ramping drives an MCU pin with nothing in between. That is the one unguarded path in the link, and it is the receive path that origin89hq/hardware#5 has not been able to rule out as the cause of the switch-on corruption, which is why rev B guards it too rather than waiting for the answer (origin89hq/firmware#62). Espressif's checklist says the EN RC alone is not enough when the rail is power-cycled, which is what recovery does (#14), and a supervisor on `V3V3` would never hold EN low while the module's own rail is down. The link stays on the module's UART0 and the firmware follows the board (#13) |
| **A-40** | **The debug header is 6-pin in ST's ST-LINK order: `VDD`, `SWCLK`, `GND`, `SWDIO`, `NRST`, `SWO`**, and `C23` sits next to `U7` pin 12. The `SWO` position stays in place but unconnected, silkscreened `NC` | Rev A's `H1` carries no NRST, so a probe cannot connect under reset, and firmware that reconfigures `PA13`/`PA14`, sleeps, or faults early locks it out, with no bootloader way back because `BOOT0` shares `PA14`. Rev A's only recovery is a wire to `C23`'s pad, 7.8 mm from the pin (#29). The Nucleo probe then plugs straight in. The G0's Cortex-M0+ has no trace output, so a pin labelled `SWO` would promise what the part cannot give |
| **A-41** | A **10–100 kΩ pull-up to 3.3 V on each `RS485_x_TX` net**, inside its bus cluster | Each MAX13487E drives its bus low whenever DI is low, and its own hot-swap pull-up works only at the transceiver's power-up. During any STM32 reset every I/O is an analogue input, so DI floats until firmware sets up the UART. On rev A all three RX lamps stayed lit for minutes under an image that never configured those UARTs, which is three buses held low (#28) |
| **A-42** | **One tactile switch, read by the STM32**: a spare GPIO to GND with a pull-up to `V3V3` (10 kΩ or the pin's internal one), a series resistor and ESD protection as `A-26` gives the 1-Wire input, the pin's I/O type confirmed in DS13560 and `PA11`/`PA12` left untouched; placed where the enclosure can expose it recessed or behind a pinhole; labelled per `A-33` with a name that does not read as reset. **The firmware defines the gestures, at least three distinct ones**; the board fixes none of them. No power function | The only local input on rev A is the selector, and a full event-ring dump has to be asked for by holding both selector inputs low at power-up. Rev B has no selector (`A-36`), so this button is the only local input, and both the mode and that dump move onto it (origin89hq/firmware#60). `docs/PROTOCOL.md` in origin89hq/km43 asks two things of this button, and the product adds the hold: P-066 gates enrolment on a physical press that opens a pairing window on any transport, P-117 arms the floor override only by a press pattern distinguishable from that press and from the factory-reset hold, and the hold itself is product-local. The STM32 owns all three because it holds the ESP32's rail and both records (#11). A power button would only add a way to switch the interlock off by mistake, and a recessed button on a wall people touch is an ESD entry like the selector |

### Power

| ID | Net | V | A | Note |
|---|---|---|---|---|
| **A-20** | `V_IN_RAW` / `V_BANK` | 12 or 24 | 2.0 | **Rev B runs from a 12 V or a 24 V bank, and everything past the front end runs from a regulated 12 V** (`A-20e`). State the maximum continuous input the board supports, the highest voltage a 24 V bank's charger applies with equalisation included, about 29 V and up, and build the chain from it, in this order from `CN1`: `F1`; a **bidirectional** TVS at the connector (`A-11`) with its standoff above that maximum (a 33 V-class part clamps near 53 V); then reverse-polarity protection the TVS cannot bypass, a P-FET or an ideal-diode controller whose drain-source rating exceeds the TVS clamp and whose gate is clamped within its gate-source rating at the maximum input, as rev A does with `D2` (BZT52C10) and `R1` on `Q1`'s gate; then the front-end buck of `A-20e`, the only converter that sees the bank, with a **60 V-class** input rating so the clamp lands under it; every capacitor on `V_BANK` and `F1` rated for the maximum input. The house-bank divider `R42`/`R43` (`A-29`) reads `V_BANK`, not the regulated rail. Trace ≥ 1.5 mm. Drawn for rev B with a maximum continuous input of 32 V: `F1` Littelfuse RXEF250, 72 V, 2.5 A hold; `D1` Diodes SMBJ33CA, bidirectional, 33 V standoff and about 53 V clamp; the reverse element is `U15` LM74700 driving `Q1` IRLML0060, a 60 V N-FET whose gate the controller's charge pump holds 12.1 to 13.9 V above the input against the FET's ±16 V rating, so no zener is needed; `C57`, `C58` 10 µF 50 V and `C59` 100 nF 100 V on `V_BANK`. A bank wired by hand in the field will be reversed once. Rev A's unidirectional `D1` sat ahead of `Q1`, so a reversed bank conducted through it, its clamp landed within 1 V of the converters' 30 V limit, and `Q1`'s 30 V rating has no margin over a reversed 24 V bank; a unidirectional TVS anywhere ahead of the reverse element reproduces that path, and a TVS behind it leaves the reversed bank and every negative transient on the FET alone (#15, #20). Rev A was built for 12 V only, with `V12` the bank itself |
| **A-20e** | `V12` | 12 | 1.5 | **The regulated 12 V rail, from one 60 V-class buck on `V_BANK`.** It feeds `U1` and `U2`, which stay rev A's TPS54331 parts and now see 12 V at most, `CN9` pin 1 (`A-20b`), the tank loop boost (`A-20d`) and any later accessory output (item 10), each output behind its own limiter. Below 12 V the buck passes the bank through, about 10.2 V at a discharged 12 V bank, which every consumer tolerates: the TPS54331 regulates from 3.5 V and board B's coils pull in at 9 V. Budget: the 5 V and 3.3 V rails at full load draw about 0.9 A from 12 V, `CN9` 0.1 A, the loop 0.05 A. `A-18`'s layout rules and the `A-16`, `A-17` and `A-28` distances apply to this converter's inductor and switch node as they do to `L1`/`L2`. Why here and not on board B: the bank voltage then reaches only the front end and this one converter; board B stays a 12 V board on any bank, which fixes its coil overvoltage (#21) and keeps a switcher out of its safety chain (#33); and one new part class replaces three. A failed buck takes the controller down as a blown `F1` does, the safe direction. Drawn for rev B: `U16` TPS54360 at about 485 kHz (`R84` 200 kΩ), `L3` 47 µH, `D27` SS310, `C61`, `C62` 22 µF 25 V beside the rev A 10 µF parts, feedback 140 kΩ and 10 kΩ for 12.0 V, compensation 9.1 kΩ, 100 nF and 100 pF for a crossover near 8 kHz by the datasheet method, EN left floating so the converter runs above its own 4.3 V lockout and passes the bank through below 12 V |
| **A-20b** | `CN9` pin 1 | 12 | 0.1 | Feeds board B's two relay coils, about 100 mA at 12 V, over a cable that leaves the controller's box, **from the regulated rail of `A-20e` through its own active current limit with its fault readable by the MCU**, sized for that load with headroom, about 250 mA, not a PTC alone: `F1` at the inlet is otherwise the only device between the bank and a pinched cable, and tripping it loses the controller and the generator interlock it feeds (#47). `FEEDBACK` high because the coil supply is in fault is a different diagnosis from `FEEDBACK` high because board B opened the contact, and firmware can only name it if the flag exists. State with the part whether the limit latches or retries after the inrush into board B's input capacitance at plug-in, since firmware depends on which. Drawn for rev B: `U12` TPS259631, the auto-retry variant, `RILM` 3.83 kΩ for 0.22–0.27 A by the datasheet table, UVLO rising at 7.8 V and OVLO rising at 14.2 V from a 100 kΩ, 8.2 kΩ, 10 kΩ string so a rail above the coils' 14.4 V maximum is cut, `FLT` read on `PD5`. The pin carries the `A-20e` rail, **12 V nominal and not below 10 V at `CN9`**: a 12 V bank discharged to 10.5 V passes through the buck at about 10.2 V, the limiter drops tens of millivolts at 100 mA, and the under-3 m cable of 22 AWG or heavier drops under 50 mV; board B's coils pull in at 9 V, 75 % of rated, so the envelope clears them on any bank. `B-09` states the same envelope, its `+12V` label stays true, and board B has no bank-voltage work (#20, #33) |
| **A-20c** | `CN4` pin 4 | 5 | 0.3 | The 5 V pin of `A-37`, from `V5` through an active current limit **with its fault readable by the MCU**, sized for a few meters at more than 100 mA each, so a shorted meter cable cannot sag `V5`. A meter bus that is silent because its 5 V tripped is a different fault from a meter that is not answering, and the log should say which. Inside `A-21`'s budget, or raise it (#34). Drawn for rev B: `U13` TPS2553, auto-retry, `RILIM` 82 kΩ for 0.29–0.37 A by the datasheet equations, `FAULT` read on `PD6`, `D25` SMAJ5.0A on the pin |
| **A-20d** | Tank loop supply | 24 | 0.025 | A regulated, current-limited rail for a 2-wire 4–20 mA sender on `CN13`, boosted from the regulated 12 V of `A-20e`, so it is the same on either bank. From rev A's `V12` the sender saw the bank minus 3 V across `R46` at 20 mA, about 9 V on a 12 V bank and less as it discharges, under the 11–12 V minimum common transmitters need, so the reading was wrong near full scale (#36). Drawn for rev B: `U20` LM2733Y boosting `V12` to about 23.4 V (180 kΩ and 10 kΩ on `FB`, `L4` the 15 µH part of `L1`), `U21` LM317L as a 24.5 mA current source (`R90` 51 Ω) into `CN13` pin 1, `D29` SMAJ26A at the connector. At 20 mA the sender sees about 17 V after the source's drop and the 3 V across `R46`, so senders needing up to 17 V are supported; a pin 1 to pin 2 short is held at 25 mA, 0.09 W in `R46`, now a 1206 quarter-watt part |
| **A-21** | `V5` | 5 | 1.5 | RS-485 and CAN transceivers |
| **A-22** | `V3V3` | 3.3 | 0.8 | Logic + ESP32. Size for burst, not average |
| **A-23** | `V3V3_ESP` | 3.3 | 0.5 | Switched sub-rail, gated by the MCU so a wedged comms processor is recoverable without a drive. **Rev B: the switch is slew-limited, about 1 ms or slower, discharges the rail while it is off, and defaults on**: its enable is pulled up so the rail is up with `PC5` high-impedance, and `PC5` drives low to cut it (item 8 of §5, #5, #14). The controller takes ownership of the rail once it has booted and applies its policy from there: the recovery ladder's cuts (L-111 and L-112 in `docs/protocol/LINK.md` of origin89hq/km43) and a bank-voltage threshold below which it keeps the radio off, so "radio off" is always a logged decision by running firmware and never a reset's side effect. Rev A's `Q2` switched it hard and defaulted off, which nobody chose: `PC5` is high-impedance through every reset, `Q3` is off, `R19` holds `Q2` off, and `C24` cannot hold 60 mA for the milliseconds until firmware drives `PC5` again, so every STM32 reset reboots the ESP32, costs a Wi-Fi association, and is one more switch-on of the kind item 8 is open on; a crash loop at the 8 s watchdog is 450 rail cycles an hour against L-112's three deliberate ones. Cold power-on brings the rail up with `V3V3` through the switch's slew, so there is no separate inrush event at boot. The one case arguing the other way, a brown-out recovery on a weak bank where the module's first Wi-Fi burst comes before the policy runs, is bounded to under a second at about 1.3 W and is measured under item 9 rather than argued. L-114's rationale is being rewritten onto this argument (origin89hq/firmware#3). Two consequences to carry: a blank or crash-looping STM32 now leaves the radio powered until firmware runs, so the recovery ladder's cuts exist only in running firmware; and a tool that reads `PC5` must map high-impedance to on for rev B, where o89-dev reads it as not driven, which was off on rev A |
| **A-24** | `OW_VCC` | 3.3 | 0.1 | **Leaves the board on field cable** to DS18B20 probes outdoors and in the battery box. Rev A feeds it through `F2`, a 100 mA PTC; **rev B feeds it through a current-limited load switch, tens of mA, with its fault readable by the MCU, and clamps the net**. Drawn for rev B: `U14` TPS2553, auto-retry, `RILIM` 232 kΩ, the part's floor, 0.10–0.14 A by the datasheet equations, `FAULT` read on `PD8`, `D26` SMAJ5.0A on the net; a limit in the tens of milliamps needs a different part. `F2`'s 0.75 Ω minimum lets a dead short draw 4.4 A from `V3V3`, more than `U2` delivers, so the rail sags before `F2` trips (#25). Without protection a chewed or water-ingressed probe cable shorts 3.3 V and takes the whole controller down: a $3 sensor killing the machine |
| **A-25** | `VBAT_RTC` | 3.3 | <1 mA | RTC backup, OR-ed from `V3V3` and an external cell through `D11`, which stays between the cell and the rail so the cell is never charged. The cell connector sits **inboard** on rev B, a 2-pin type that suits a board-mounted lead such as the JST PH already used for `CN6`/`CN7`; rev A's `CN14` took a slot in the left-edge column it did not need (#10). External to the *board*, not to the box: the cell and its holder live inside the enclosure, so the connector is not field wiring and carries no label on the port strips. Keeps the event log's timeline across a power loss |

---

## 4. Board B — generator

Drawn and routed in-house, so this brief never went out for it. Its rules are
numbered `B-nn` in [GENERATOR-BOARD.md](../generator-b/GENERATOR-BOARD.md), beside the
schematic they constrain and the bench proof they are checked by, and that
list is the only one. A second copy here would be two documents free to
disagree, which they had already begun to do. What this file keeps about
Board B is what a layout house would need: the split in §1, the deliverables
in §2, and items 5 and 6 of §5.

---

## 5. Open: ours to close, not yours

| | What | Status |
|---|---|---|
| 1 | ~~**Enclosure and board envelope**~~ | **Closed by reversing it.** The enclosure will be 3D printed around the finished board, so the board no longer waits on a box; the box waits on the board. This makes the **STEP and DXF deliverables in §2 load-bearing**: they are the model the enclosure is built from. Printed in **MJF PA12** (nylon, dyed black), not FDM: it sits in an electrical room beside an inverter and a generator, PLA softens around 60 °C and PETG creeps under load, and nylon does neither. The box is `enclosure/shoe.py`, one script for both boards, gated against these connector envelopes |
| 2 | **MCU pin assignment** | **Closed for rev A; reopened for rev B by `A-34`.** Every assignment checked against `stm32-metapac 21.0.0`, machine-extracted from ST's database by `stm32-data` and already a dependency of this firmware. USART1 CTS moved to **PB4/AF4**, so the ESP32 link sits entirely on PB6/PB7/PB4/PB3 and the PA11/PA12 dual-identity pads are untouched. FDCAN1 confirmed on PB9/PB8 at AF3. No two peripherals contend for a pin. See [*Before any board is laid out*](../../docs/DESIGN.md#before-any-board-is-laid-out) in `docs/DESIGN.md`. Rev B moves `RUN`, `KICK` and `FEEDBACK` off `PD0`–`PD2` and adds the button of `A-42`; each new pin is checked against AN2606 Rev 70 table 111 as well as the metapac, `FEEDBACK`'s new pin has an I/O type that takes the firmware's pull-up (`B-09b`), and rev A's 19 unused pins are the candidates (#11, #30) |
| 3 | ~~**5 V-tolerant RX pins**~~ | **Closed.** MAX13487E is a 5 V part, so `RO` drives 5 V logic into the three RS-485 RX pins. All three are `FT` per DS13560 §3.4: `PA3` **FT_ea**, `PB11` **FT_fa**, `PA1` **FT_ea**. **Note for anyone moving a pin later: `PA4` is `TT_a` and `PA5` is `TT_ea`, 3.3 V only.** They carry SPI to the NOR, which is a 3.3 V part, so they are safe here. Put a 5 V signal on either and the board is scrap |
| 4 | ~~**Relay contact material**~~ | **Closed for the class, still to confirm for the site.** A sweep of the remote-start section of every 2-wire generator controller's manual found each input is switched-to-negative at a few milliamps behind a pull-up (the dry-circuit range where plain silver contacts fail to make through their oxide film), so Board B carries the G5V-2's gold-clad bifurcated contacts, specified from 10 µA. A meter across the GenStart input (open-circuit V, short-circuit I) still confirms this site sits inside that class; it no longer decides the part |
| 5 | **How the 60 ft run is routed** | Buried, aerial or conduit. Aerial means surge protection sized for it; buried means rodents. Decides how hard GENERATOR-BOARD.md's `B-06` (the TVS at CN10) has to be |
| 6 | **Does the garage share the cabin's earthing system?** | Attached and on the same panel means one ground and nothing to do. A detached garage with its own rod means the pair bridges two earthing systems |
| 7 | ~~**RTC accuracy**~~ | **Closed.** `X2` (32.768 kHz, ±20 ppm, −40…+85 °C) is fitted with `C41`/`C42`, and `D11` ORs `V3V3` with an external cell on `CN14` into `VBAT`. ±20 ppm is ~1.7 s/day; the ESP32 can NTP-discipline it whenever Wi-Fi is up. A ±2 ppm TCXO RTC was considered and rejected at 35× the price for accuracy nothing here consumes |
| 8 | **Inrush into `V3V3_ESP`** | **Open, from the rev A bench.** Rev A's `Q2` connects `C24` and the module to `V3V3` in what the bench estimated as nanoseconds, a figure taken from charge sharing rather than measured: `C24` was not scoped and the inrush and receive-path causes were not separated, so the mechanism is still a hypothesis. After 10 minutes off, every switch-on corrupted the MCU (22 of 22); switch-ons a few seconds or 30 s apart all passed. `A-23` needs a slew-controlled switch for rev B, about 1 ms or slower as a candidate target until a waveform and a controlled rework confirm it: a TPS22918 with `CT` = 1 nF, or `Q2` with a gate resistor and a gate-drain capacitor. The `Q2` rework is default-off by construction, `R19` holding the P-FET off, so what it proves is the slew; `A-23`'s default-on comes with the rev B part, whose enable is pulled up, which reverses the `ON` pull-down #5 proposed and is recorded there. Close when a rev A rework passes 10-minute off-times. Tracked in [origin89hq/hardware#5](https://github.com/origin89hq/hardware/issues/5); evidence in `bench/2026-09-14.md`. That rework is the first item on the rev A bench before the rev B order: if slew alone does not fix it, `A-31`'s decoupling and the USART1 receive path are next |
| 9 | **Values settled on the rev A bench before the rev B order** | The bias (`A-13c`, #26), the LED current (`A-14`, #35), the rail switch (item 8, #5), the IO8 pull-up (`A-39`, #6), and `V3V3_ESP` under a Wi-Fi burst through the new switch, which sets the EN supervisor's threshold (`A-39`, #14). Also `V12` and `V3V3_ESP` scoped through a supply-limited brown-out recovery with the ESP rail forced on and forced off, checking that the module's start does not pull `V12` back under the buck's stop threshold (8.0 V on rev A); the 2026-09-16 brown-outs were this scenario with the rail unscoped (`A-23`). And a real Victron product on a VE.Direct port: meter the cable's pin 2 and pin 3 against the product's table, then receive a text frame (`A-38`, #27); no Victron device has been on the bench. Each issue carries its bench test. The `CN9` erase test of #30 runs only on a spare board with nothing on board B's `CN10` |
| 10 | **Accessory bus and 12 V output (#46)** | **Not in rev B.** The research record stands, and the heater path exists without a board change: a Modbus module driving an interposer relay. The right edge keeps the space #46 measured, about 33 mm clear above `CN11` and 15 mm between `CN7` and `CN9`, so a later revision can add the output under `A-06b`. If it is added it follows `A-20b`: from the regulated rail of `A-20e`, its own active limit, its own budget row, and a connector that cannot mate with the 3.5 mm sockets |
| 11 | ~~**48 V banks**~~ | **Closed: out of scope, decided 2026-09-18.** A 48 V site already runs its 24 V loads, fridges among them, from a 48-to-24 V converter of its own, and the controller connects to that 24 V. The `A-20e` front end stays 60 V-class. A direct 48 V input would have cost a 100 V-class front end, TVS and reverse element, with the clamp tight against the converter's rating, and a doubled house divider (`A-29`); none of that is bought. One consequence for the firmware, not the board: on such a site the house-bank input reads the converter's output, not the bank, so the bank state has to come from a meter on a bus, a BMV on VE.Direct or a shunt over Modbus, and the site configuration says which |

**No isolated RS-485 channel is fitted, deliberately.** The criterion is two
separate earthing systems, not distance. At site A the generator's 120 V output
already runs to the cabin, so proof of running is measured at the panel and
**nothing but the dry contact pair crosses to the garage**. An ADM2582E and an
8 mm barrier would cost board area for a bus with no consumer. Item 6 above is
what would reopen this.

---

## 6. Acceptance

A layout is accepted when:

- Every `A-nn` and `B-nn` is met, or was raised and a decision recorded.
- DRC clean at the fab's stated capability, with the rule set delivered.
- The fabrication drawing shows **achieved** impedance for `A-08` and `A-09`, not target.
- A ground-plane view of L2 shows no split under any differential pair (`A-05`).
- The antenna keep-out of `A-15` is visible in the delivered Gerbers, checked
  layer by layer rather than asserted.
- The export's net names match the schematic labels, with no `NET_n` on an MCU
  net, and the `SWCLK`/`PA14` net carries only the debug header, `R17` and `U7`.
  The 2026-09-09 export lost five labels, both `CN9` command nets among them,
  and its schematic has a stray wire that would join `SWCLK` to `V3V3` under a
  name the first check accepts (#9).
- `tools/validate_silkscreen.py` passes against the export's `board.dxf` (`A-33`).

The antenna and ground-plane items are the ones to check by looking, not by
trusting the report. Both are failures that pass DRC.
